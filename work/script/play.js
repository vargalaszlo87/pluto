// play event's
const play = {
    descriptive: (event, dataID) => {
        return descriptiveStatistic.play(event, dataID);
    },
    descriptiveJS: () => {
        descriptiveStatistic.playJS();
    }
}